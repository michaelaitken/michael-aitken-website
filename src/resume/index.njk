---
layout: layouts/base.njk
title: Resume
subtitle: Systems & Backend Software Engineer
---

<section class="resume-header">
  <div class="resume-intro">
    <p>
      Systems engineer with manufacturing domain expertise, transitioning to backend 
      and systems software development. Strong foundation in modern C++, embedded systems, 
      and infrastructure design.
    </p>
  </div>
  
  <div class="resume-actions">
    <a href="/contact/" class="btn btn-primary">Get in Touch</a>
  </div>
</section>

<section class="resume-section">
  <h2>Core Competencies</h2>
  
  <div class="skills-grid">
    <div class="skill-category">
      <h3>Primary Focus</h3>
      <ul class="skill-list skill-list-primary">
        {% for skill in skills.primary %}
        <li>
          <span class="skill-name">{{ skill.name }}</span>
          <span class="skill-level">{{ skill.level }}</span>
        </li>
        {% endfor %}
      </ul>
    </div>
    
    <div class="skill-category">
      <h3>Technical Skills</h3>
      <div class="skill-tags">
        {% for skill in skills.technical %}
        <span>{{ skill }}</span>
        {% endfor %}
      </div>
    </div>
    
    <div class="skill-category">
      <h3>Domain Knowledge</h3>
      <div class="skill-tags">
        {% for skill in skills.domain %}
        <span>{{ skill }}</span>
        {% endfor %}
      </div>
    </div>
    
    <div class="skill-category">
      <h3>Professional Skills</h3>
      <div class="skill-tags">
        {% for skill in skills.soft %}
        <span>{{ skill }}</span>
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<section class="resume-section">
  <h2>Experience</h2>
  
  <div class="timeline">
    {% for item in experience %}
      {% include "components/timeline-item.njk" %}
    {% endfor %}
  </div>
</section>

<section class="resume-section">
  <h2>Education</h2>
  
  <div class="education-list">
    {% for edu in education %}
    <div class="education-item">
      <div class="education-header">
        <h3>
          {{ edu.degree }}
          {% if edu.distinction %}<span class="distinction">with Distinction</span>{% endif %}
        </h3>
        <span class="education-period">{{ edu.period }}</span>
      </div>
      <p class="education-institution">{{ edu.institution }}</p>
      {% if edu.minor %}
      <p class="education-minor">Minor: {{ edu.minor }}</p>
      {% endif %}
      {% if edu.notes %}
      <p class="education-notes">{{ edu.notes }}</p>
      {% endif %}
    </div>
    {% endfor %}
  </div>
</section>

<section class="resume-section">
  <h2>Recognition</h2>
  
  <div class="awards">
    <div class="award-item">
      <div class="award-icon">★</div>
      <div class="award-content">
        <h3>"Be Bold" Award</h3>
        <p>Inaugural National Achievement Awards, 2025</p>
        <p class="award-desc">
          Recognized for initiative and impact in driving the SCADA infrastructure 
          modernization project.
        </p>
      </div>
    </div>
  </div>
</section>